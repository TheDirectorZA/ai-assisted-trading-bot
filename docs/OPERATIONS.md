# Operations

Common commands:

```bash
make migrate
make seed
make backtest
make test
make lint
make typecheck
make mt5-check
make sync-account
make sync-positions
make kill-switch
```

Dangerous commands require exact confirmation phrases and write audit logs.
